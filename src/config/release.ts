import {Config} from "@/config/types";

// 生产环境配置
const config: Partial<Config> = {
  baseUrl: 'https://client-gw.cogolinks.com',
  merchantBaseUrl: 'https://cn.cogolinks.com',
}

export default config
